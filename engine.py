def encriptBas(texto):
    texto_completo = ""
    lista = []
    for i in range(len(texto)):
        texto_acortado = texto[i:i+1]
        if texto_acortado == "a":
            texto_acortado = "m"
            lista.append(texto_acortado)

        elif texto_acortado == "b":
            texto_acortado = "ñ"
            lista.append(texto_acortado)

        elif texto_acortado == "c":
            texto_acortado = "r"
            lista.append(texto_acortado)

        elif texto_acortado == "d":
            texto_acortado = "t"
            lista.append(texto_acortado)

        elif texto_acortado == "e":
            texto_acortado = "y"
            lista.append(texto_acortado)

        elif texto_acortado == "f":
            texto_acortado = "z"
            lista.append(texto_acortado)

        elif texto_acortado == "g":
            texto_acortado = "w"
            lista.append(texto_acortado)

        elif texto_acortado == "h":
            texto_acortado = "q"
            lista.append(texto_acortado)

        elif texto_acortado == "i":
            texto_acortado = "b"
            lista.append(texto_acortado)

        elif texto_acortado == "j":
            texto_acortado = "u"
            lista.append(texto_acortado)

        elif texto_acortado == "k":
            texto_acortado = "a"
            lista.append(texto_acortado)

        elif texto_acortado == "l":
            texto_acortado = "o"
            lista.append(texto_acortado)

        elif texto_acortado == "m":
            texto_acortado = "k"
            lista.append(texto_acortado)

        elif texto_acortado == "n":
            texto_acortado = "v"
            lista.append(texto_acortado)

        elif texto_acortado == "ñ":
            texto_acortado = "p"
            lista.append(texto_acortado)

        elif texto_acortado == "o":
            texto_acortado = "i"
            lista.append(texto_acortado)

        elif texto_acortado == "p":
            texto_acortado = "l"
            lista.append(texto_acortado)

        elif texto_acortado == "q":
            texto_acortado = "c"
            lista.append(texto_acortado)

        elif texto_acortado == "r":
            texto_acortado = "d"
            lista.append(texto_acortado)

        elif texto_acortado == "s":
            texto_acortado = "x"
            lista.append(texto_acortado)

        elif texto_acortado == "t":
            texto_acortado = "j"
            lista.append(texto_acortado)

        elif texto_acortado == "u":
            texto_acortado = "e"
            lista.append(texto_acortado)

        elif texto_acortado == "v":
            texto_acortado = "g"
            lista.append(texto_acortado)

        elif texto_acortado == "w":
            texto_acortado = "s"
            lista.append(texto_acortado)

        elif texto_acortado == "x":
            texto_acortado = "n"
            lista.append(texto_acortado)

        elif texto_acortado == "y":
            texto_acortado = "h"
            lista.append(texto_acortado)

        elif texto_acortado == "z":
            texto_acortado = "f"
            lista.append(texto_acortado)

        elif texto_acortado == "A":
            texto_acortado = "M"
            lista.append(texto_acortado)

        elif texto_acortado == "B":
            texto_acortado = "Ñ"
            lista.append(texto_acortado)

        elif texto_acortado == "C":
            texto_acortado = "R"
            lista.append(texto_acortado)

        elif texto_acortado == "D":
            texto_acortado = "T"
            lista.append(texto_acortado)

        elif texto_acortado == "E":
            texto_acortado = "Y"
            lista.append(texto_acortado)

        elif texto_acortado == "F":
            texto_acortado = "Z"
            lista.append(texto_acortado)

        elif texto_acortado == "G":
            texto_acortado = "W"
            lista.append(texto_acortado)

        elif texto_acortado == "H":
            texto_acortado = "Q"
            lista.append(texto_acortado)

        elif texto_acortado == "I":
            texto_acortado = "B"
            lista.append(texto_acortado)

        elif texto_acortado == "J":
            texto_acortado = "U"
            lista.append(texto_acortado)

        elif texto_acortado == "K":
            texto_acortado = "A"
            lista.append(texto_acortado)

        elif texto_acortado == "L":
            texto_acortado = "O"
            lista.append(texto_acortado)

        elif texto_acortado == "M":
            texto_acortado = "K"
            lista.append(texto_acortado)

        elif texto_acortado == "N":
            texto_acortado = "V"
            lista.append(texto_acortado)

        elif texto_acortado == "Ñ":
            texto_acortado = "P"
            lista.append(texto_acortado)

        elif texto_acortado == "O":
            texto_acortado = "I"
            lista.append(texto_acortado)

        elif texto_acortado == "P":
            texto_acortado = "L"
            lista.append(texto_acortado)

        elif texto_acortado == "Q":
            texto_acortado = "C"
            lista.append(texto_acortado)

        elif texto_acortado == "R":
            texto_acortado = "D"
            lista.append(texto_acortado)

        elif texto_acortado == "S":
            texto_acortado = "X"
            lista.append(texto_acortado)

        elif texto_acortado == "T":
            texto_acortado = "J"
            lista.append(texto_acortado)

        elif texto_acortado == "U":
            texto_acortado = "E"
            lista.append(texto_acortado)

        elif texto_acortado == "V":
            texto_acortado = "G"
            lista.append(texto_acortado)

        elif texto_acortado == "W":
            texto_acortado = "S"
            lista.append(texto_acortado)

        elif texto_acortado == "X":
            texto_acortado = "N"
            lista.append(texto_acortado)

        elif texto_acortado == "Y":
            texto_acortado = "H"
            lista.append(texto_acortado)

        elif texto_acortado == "Z":
            texto_acortado = "F"
            lista.append(texto_acortado)









        elif texto_acortado == "1":
            texto_acortado = "?"
            lista.append(texto_acortado)

        elif texto_acortado == "2":
            texto_acortado = ")"
            lista.append(texto_acortado)

        elif texto_acortado == "3":
            texto_acortado = "%"
            lista.append(texto_acortado)

        elif texto_acortado == "4":
            texto_acortado = "!"
            lista.append(texto_acortado)

        elif texto_acortado == "5":
            texto_acortado = "@"
            lista.append(texto_acortado)

        elif texto_acortado == "6":
            texto_acortado = "{"
            lista.append(texto_acortado)

        elif texto_acortado == "7":
            texto_acortado = "`"
            lista.append(texto_acortado)

        elif texto_acortado == "8":
            texto_acortado = "#"
            lista.append(texto_acortado)

        elif texto_acortado == "9":
            texto_acortado = "´"
            lista.append(texto_acortado)








        elif texto_acortado == ".":
            texto_acortado = "6"
            lista.append(texto_acortado)

        elif texto_acortado == ",":
            texto_acortado = "9"
            lista.append(texto_acortado)

        elif texto_acortado == ";":
            texto_acortado = "ç"
            lista.append(texto_acortado)

        elif texto_acortado == "-":
            texto_acortado = "5"
            lista.append(texto_acortado)

        elif texto_acortado == "_":
            texto_acortado = ";"
            lista.append(texto_acortado)

        elif texto_acortado == "+":
            texto_acortado = "4"
            lista.append(texto_acortado)

        elif texto_acortado == "*":
            texto_acortado = "$"
            lista.append(texto_acortado)

        elif texto_acortado == "ç":
            texto_acortado = ","
            lista.append(texto_acortado)

        elif texto_acortado == "Ç":
            texto_acortado = "7"
            lista.append(texto_acortado)

        elif texto_acortado == "/":
            texto_acortado = "*"
            lista.append(texto_acortado)

        elif texto_acortado == "=":
            texto_acortado = "2"
            lista.append(texto_acortado)

        elif texto_acortado == "?":
            texto_acortado = "º"
            lista.append(texto_acortado)

        elif texto_acortado == "¿":
            texto_acortado = "3"
            lista.append(texto_acortado)

        elif texto_acortado == "¡":
            texto_acortado = "+"
            lista.append(texto_acortado)

        elif texto_acortado == "!":
            texto_acortado = "_"
            lista.append(texto_acortado)

        elif texto_acortado == "@":
            texto_acortado = "1"
            lista.append(texto_acortado)

        elif texto_acortado == "#":
            texto_acortado = "Ç"
            lista.append(texto_acortado)

        elif texto_acortado == "$":
            texto_acortado = "8"
            lista.append(texto_acortado)

        elif texto_acortado == "(":
            texto_acortado = "."
            lista.append(texto_acortado)

        elif texto_acortado == ")":
            texto_acortado = "-"
            lista.append(texto_acortado)

        elif texto_acortado == " ":
            texto_acortado = "~"
            lista.append(texto_acortado)

        texto_completo =  texto_completo + texto_acortado

    return texto_completo

if __name__ == "__main__":
    print(encriptBas("Hola, esto es una prueba"))